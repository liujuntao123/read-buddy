{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window capability for read-buddy",
  "windows": ["main"],
  "permissions": ["core:default"]
}
