'use client';

/**
 * Placeholder body for the 对话 tab. Replaced by the real chat UI in
 * ticket 04 — keep `data-testid="chat-tab-panel"` stable for that handoff.
 */
export default function ChatTabPlaceholder() {
  return (
    <div
      data-testid="chat-tab-panel"
      className="rounded-box border border-dashed border-base-300 p-4 text-sm text-base-content/60"
    >
      伴读对话 — 工单 04 实施中
    </div>
  );
}
