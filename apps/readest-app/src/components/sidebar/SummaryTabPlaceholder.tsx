'use client';

/**
 * Placeholder body for the 总结 tab. Replaced by the real summarizer UI in
 * ticket 03 — keep `data-testid="summary-tab-panel"` stable for that handoff.
 */
export default function SummaryTabPlaceholder() {
  return (
    <div
      data-testid="summary-tab-panel"
      className="rounded-box border border-dashed border-base-300 p-4 text-sm text-base-content/60"
    >
      章节总结 — 工单 03 实施中
    </div>
  );
}
