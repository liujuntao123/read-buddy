{
  "name": "read-buddy",
  "private": true,
  "version": "0.2.0",
  "description": "Zero-embedding, chapter-first desktop AI reading companion built on readest architecture",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/liujuntao123/read-buddy.git"
  },
  "scripts": {
    "dev": "pnpm --filter read-buddy-app dev",
    "build": "pnpm --filter read-buddy-app build",
    "build:installer": "pnpm --filter read-buddy-app tauri build",
    "tauri": "pnpm --filter read-buddy-app tauri",
    "verify": "pnpm version:check && pnpm typecheck && pnpm test && pnpm build",
    "typecheck": "pnpm --filter read-buddy-app typecheck",
    "test": "pnpm --filter read-buddy-app test",
    "test:watch": "pnpm --filter read-buddy-app test:watch",
    "version:show": "node scripts/version.mjs",
    "version:check": "node scripts/version.mjs check",
    "version:set": "node scripts/version.mjs set",
    "version:bump": "node scripts/version.mjs bump",
    "changelog": "node scripts/changelog.mjs",
    "release": "node scripts/release.mjs"
  },
  "packageManager": "pnpm@12.4.2"
}
